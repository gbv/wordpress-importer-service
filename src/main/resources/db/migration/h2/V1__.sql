CREATE TABLE BLOG_POST_INFO
(
    ID           BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    BLOG         VARCHAR(255)                            NOT NULL,
    DATE         TIMESTAMP                               NOT NULL,
    TITLE        VARCHAR(1000)                           NOT NULL,
    URL          VARCHAR(1000)                           NOT NULL,
    WORDPRESS_ID BIGINT                                  NOT NULL,
    CONSTRAINT CONSTRAINT_7 PRIMARY KEY (ID)
);

CREATE TABLE MYCORE_OBJECT_INFO
(
    ID               BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    CREATED          TIMESTAMP,
    FULLTEXT         VARCHAR(1000),
    LAST_MODIFIED    TIMESTAMP,
    MYCORE_ID        VARCHAR(20)                             NOT NULL,
    PARENT_MYCORE_ID VARCHAR(20)                             NOT NULL,
    REPOSITORY       VARCHAR(255)                            NOT NULL,
    STATE            VARCHAR(255)                            NOT NULL,
    WORDPRESS_ID     BIGINT,
    WORDPRESS_URL    VARCHAR(1000),
    CONSTRAINT CONSTRAINT_A PRIMARY KEY (ID)
);

ALTER TABLE MYCORE_OBJECT_INFO
    ADD CONSTRAINT UKGH23WB2M9XS5FNH9EMPH35N0S UNIQUE (MYCORE_ID);

ALTER TABLE BLOG_POST_INFO
    ADD CONSTRAINT UKK5XB5S15PMJ9NRLKP1FAPYPAK UNIQUE (WORDPRESS_ID, BLOG);